import 'package:flutter/material.dart';

class CampusNavigation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Not added yet'),
      ),
    );
  }
}
